    <h1> Résultat de la recherche</h1>
    <div>
        {% if tracks is not empty %}
            {% for track in tracks %}
                <p>{{  track.name }}</p>
            {% endfor %}
        {% endif %}
    </div>

